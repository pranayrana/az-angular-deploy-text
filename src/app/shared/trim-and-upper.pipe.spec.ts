import { TrimAndUpperPipe } from './trim-and-upper.pipe';

describe('TrimAndUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimAndUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
